import { Middleware } from '@commercetools/sdk-types';
declare type UserAgentMiddlewareOptions = {
    libraryName?: string;
    libraryVersion?: string;
    contactUrl?: string;
    contactEmail?: string;
};
export default function createUserAgentMiddleware(options: UserAgentMiddlewareOptions): Middleware;
export {};
